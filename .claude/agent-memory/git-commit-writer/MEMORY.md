# Agent Working Rules

- Run unit tests for every code change before finalizing.
- Use git for all changes and create commits (no uncommitted final state).
- Keep a short summary of changes in `CHANGE_SUMMARY.md`.
- Update this memory file when workflow rules change.
